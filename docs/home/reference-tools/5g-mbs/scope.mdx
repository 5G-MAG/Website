---
title: Scope
hide_title: true
sidebar_position: 0
description: Per-feature audit of what the 5G MBS reference tools implement across the MBS User Services, 5GC, NG-RAN and UE layers, mapped against 3GPP specifications.
---

import useBaseUrl from '@docusaurus/useBaseUrl';

<div class="topic-banner">
<div class="topic-banner__icon-wrap">
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path stroke="none" d="M0 0h24v24H0z" fill="none" />
  <path d="M12 12l0 .01" />
  <path d="M14.828 9.172a4 4 0 0 1 0 5.656" />
  <path d="M17.657 6.343a8 8 0 0 1 0 11.314" />
  <path d="M9.168 14.828a4 4 0 0 1 0 -5.656" />
  <path d="M6.337 17.657a8 8 0 0 1 0 -11.314" /></svg>
</div>
<div class="topic-banner__text">
<span class="topic-banner__kicker">5G Multicast Broadcast Services (MBS)</span>
<h1>Scope</h1>
</div>
</div>

<div style={{margin: '8px 0'}}><a class="button button--primary" href={useBaseUrl('/reference-tools/5g-mbs/scope')} style={{margin: '2px 4px 2px 0'}}>Scope</a> <a class="button button--outline button--primary" href={useBaseUrl('/reference-tools/5g-mbs/resources')} style={{margin: '2px 4px 2px 0'}}>Resources</a> <a class="button button--outline button--primary" href={useBaseUrl('/reference-tools/5g-mbs/tutorials')} style={{margin: '2px 4px 2px 0'}}>Tutorials</a> <a class="button button--outline button--primary" href={useBaseUrl('/reference-tools/5g-mbs/tutorials#developer-exchange')} style={{margin: '2px 4px 2px 0'}}>Developer Exchange</a></div>

**Category:** 3GPP Implementations

## What This Project Implements

These reference tools implement parts of the 3GPP 5G Multicast Broadcast Services (MBS) feature. They are aimed at developers and integrators who want to run and inspect MBS delivery over a 5G Core, using multicast and broadcast to send the same media to many devices at once. To try the tools hands-on, see the [Tutorials](./tutorials).

:::note[Key terms]

- **5GC**: 5G Core network. **NG-RAN**: the 5G radio access network. **UE**: User Equipment (the device).
- **MBSF / MBSTF**: MBS Function (control) and MBS Transport Function (media data plane), the two MBS User Services network functions.
- **MB-SMF / MB-UPF**: the multicast/broadcast versions of the Session Management Function and User Plane Function in the 5G Core.
- **AF / AS**: the Application Function and Application Server on the MBS Application Provider side.
- **TMGI**: Temporary Mobile Group Identifier, the identifier for an MBS session. **SSM**: Source-Specific Multicast address.
- **FLUTE / ALC**: File Delivery over Unidirectional Transport, carried in Asynchronous Layered Coding packets, used to send files one-way.
- **PFCP**: Packet Forwarding Control Protocol (control between MB-SMF and MB-UPF). **GTP-U**: the user-plane tunnelling protocol between core and RAN.
- **PTM**: Point-to-Multipoint delivery. **NTN**: Non-Terrestrial Network (satellite) access.
- **MRB**: MBS Radio Bearer, the radio bearer carrying broadcast traffic. **MCCH / MTCH**: the MBS Control and Traffic logical channels &mdash; MCCH announces the live sessions over the air, MTCH carries their content. **G-RNTI**: the group radio identity a session's MTCH is scheduled on.
  :::

In the context of 3GPP standards, the Multicast Broadcast Services System, **MBS System**, is designed to support multicast and broadcast services in 5G networks. It introduces a flexible architecture that supports both Multicast (for specific groups with high reliability) and Broadcast (for wide-area distribution to all users), allowing the network to dynamically switch between point-to-point and point-to-multipoint delivery. While the MBS System handles the heavy lifting of moving data through the 5G core and radio, **MBS User Services** defines how that content is actually packaged, discovered, and consumed by the end-user's application.

Technical documentation providing context to this project can be found in the link below.

[Tech: Multicast and Broadcast Services in 5G Networks](/tech/5g-mbs)

Relevant specifications are listed below.

[Standards: 5G Multicast Broadcast Services](/standards/5g-mbs)

[Standards: Multimedia Delivery Protocols](/standards/multimedia)

:::tip[In short]
The complete MBS Broadcast delivery chain: the MBS Application Provider side, the MBS User Services network functions (MBSF, MBSTF), the 5GC (MB-SMF, MB-UPF, AMF), the NG-RAN (gNB), and the UE side (modem, MBSF Client and MBS-Aware Application). MBS Multicast is not yet implemented.
:::

## What's implemented, feature by feature

Every functional entity in the MBS Broadcast chain is audited per feature against its defining specifications. Each section below is split into plainly labelled groups, so you can jump straight to the component you care about.

:::note[Audit baseline]
This describes the `development` branches of the MBS User Services repositories and the `5mbs-development` branches of the Open5GS and srsRAN forks as of 2026-08-12, including the changes currently in review. It is a source-level reading of the code against the specification text, backed by live end-to-end testing of the MBS Broadcast delivery chain (portal &rarr; MBSF &rarr; MBSTF &rarr; MB-SMF/MB-UPF &rarr; gNB &rarr; UE &rarr; MBSF Client &rarr; MBS-Aware Application), not a maintainer sign-off.
:::

:::caution[Broadcast first, Multicast later]
The tools currently implement the **MBS Broadcast** delivery mode end-to-end. The **MBS Multicast** delivery mode (UE session join via the SMF, PTP/PTM switching, group paging) is not yet implemented in any component of the chain &mdash; the MB-SMF/MB-UPF multicast data path (N6mb ingest, shared GTP-U delivery) exists, but the session-join control plane does not.
:::

### Components and where they sit

| Component | Repository | Layer | Interfaces / reference points |
| --- | --- | --- | --- |
| MBS Application Provider portal | rt-mbs-application-provider | Application provider | Nmb10 consumer (towards MBSF), Nmb8 content push (towards MBSTF) |
| MBSF | rt-mbs-function | MBS User Services | Nmb10 server, Nmb2 consumer (towards MBSTF), Nmb1 consumer (towards MB-SMF), MBS-4-MC announcement carousel |
| MBSTF | rt-mbs-transport-function | MBS User Services | Nmb2 server, Nmb8 object ingest, Nmb9 FLUTE/ALC delivery (towards MB-UPF) |
| MB-SMF | Open5GS fork | 5G Core | Nmb1 server, N4mb PFCP (towards MB-UPF), Namf_MBSBroadcast consumer (towards AMF) |
| MB-UPF | Open5GS fork | 5G Core | N4mb PFCP server, N6mb multicast ingest, Nmb9 tunnel ingest, N3mb GTP-U shared delivery (towards gNBs) |
| AMF | Open5GS fork | 5G Core | Namf_MBSBroadcast server, NGAP N2 (towards gNB) |
| gNB (CU-CP / CU-UP / DU) | srsRAN Project fork | NG-RAN | NGAP N2, E1AP, F1AP, N3mb GTP-U termination, Uu broadcast (MCCH/MTCH) |
| UE modem | srsRAN 4G fork (`srsue`) | UE | Uu broadcast reception (SIB20/MCCH/MTCH), TUN delivery to the MBSF Client |
| MBSF Client | rt-mbs-client | UE | MBS-4-MC FLUTE reception, MBS-6/MBS-7 local APIs (towards the MBS-Aware Application) |
| MBS-Aware Application | rt-mbs-application | UE | MBS-6/MBS-7 consumer (dashboard UI) |

### TS 29.580 &mdash; MBSF services at Nmb10

Server: **MBSF (rt-mbs-function)**. Consumer: **MBS Application Provider portal (rt-mbs-application-provider)**. Sits between the MBS Application Provider and the MBSF.

| Service operation | Status |
| --- | --- |
| MBS User Service: create (POST `/mbs-user-services`) | Implemented |
| MBS User Service: retrieve individual (GET `.../{userServiceId}`) | Implemented |
| MBS User Service: retrieve collection (GET `/mbs-user-services`) | Not implemented (rejected as an invalid resource; the portal keeps its own registry instead) |
| MBS User Service: update (PUT) | Implemented |
| MBS User Service: modify (PATCH, RFC 7396) | On hold pending [Standards#182](https://github.com/5G-MAG/Standards/issues/182) ([issue #45](https://github.com/5G-MAG/rt-mbs-function/issues/45)) &mdash; use PUT |
| MBS User Service: delete | Implemented |
| CreateMBSUserDataIngSession (POST `/sessions`) | Implemented |
| RetrieveIndMBSUserDataIngSession (GET `.../{sessionId}`) | Implemented |
| RetrieveMBSUserDataIngSessions (GET `/sessions`) | Not implemented (same collection-endpoint gap as above) |
| UpdateIndMBSUserDataIngSession (PUT) | Implemented, with the &sect;5.3.2.4.2 update rules enforced: create-only attributes (`mbsSessionId`, `mbsDistSessionId`, `locationDependent`) protected; INACTIVE-only attributes (`objDistrInfo`, `pckDistrInfo`, `maxContBitRate`, ...) rejected while active; any-time attributes (`mbsServInfo`, `tgtServAreas`, `extTgtServAreas`, `nrRedCapUeInfo`, ...) applied live |
| ModifyIndMBSUserDataIngSession (PATCH, RFC 7396) | On hold (same as above) |
| DeleteIndMBSUserDataIngSession | Implemented |
| Scheduled activation (`actPeriods` / `actPeriodsRepRule`, mutually exclusive) | Implemented |
| CreateMBSUserDataIngStatSubsc (POST `/status-subscriptions`) | Implemented |
| RetrieveMBSUserDataIngStatSubscs / RetrieveInd (GET) | Implemented (returned objects carry no id of their own &mdash; see [Standards#191](https://github.com/5G-MAG/Standards/issues/191)) |
| UpdateIndMBSUserDataIngStatSubsc (PUT) / ModifyInd (PATCH) | Implemented |
| DeleteMBSUserDataIngStatSubsc | Implemented |
| MBSUserDataIngStatNotif (notification to the provider callback URL, &sect;6.2.5) | Implemented |

### TS 26.502 / TS 26.517 &mdash; MBS User Services procedures and formats

Implemented across **MBSF** (announcement generation), **MBSTF** (object delivery) and **MBSF Client / MBS-Aware Application** (reception and consumption).

| Feature | Where | Status |
| --- | --- | --- |
| Service Announcement mode `PASSED_BACK` &mdash; bundle handed back to the Application Provider at Nmb10 for out-of-band distribution (not delivered over any MBS reference point) | MBSF | Implemented |
| Service Announcement mode `VIA_MBS_DISTRIBUTION_SESSION` &mdash; MBSF's own broadcast announcement carousel, delivered **over MBS-4-MC** (multicast/broadcast) on a fixed, configurable SSM address/port | MBSF generates + MBSTF transmits; MBSF Client receives | Implemented |
| Service Announcement mode `VIA_MBS_5` &mdash; unicast retrieval by the client **over MBS-5** | &mdash; | Not implemented (no MBS-5 network API) |
| User Service Description bundle, `multipart/related` (TS 26.517 &sect;5.3.1A): `UserServiceDescriptions` document plus one SDP per Distribution Session | MBSF generates; MBSF Client decomposes and parses | Implemented, including document version ordering (&sect;5.2.2) and relative Content-Location resolution (RFC 2557) |
| `ServiceScheduleDescription` activation time windows (&sect;5.2.7) | MBSF Client | Implemented |
| Session Description / SDP for FLUTE sessions (&sect;6.2.2): `m=` FLUTE lines, `flute-tsi`, SSM `source-filter`, `mbs-servicetype`, timing | MBSF generates; MBSF Client parses | Implemented (multi-session SDPs supported) |
| Object distribution method over FLUTE, per the TS 26.346 Download profile (&sect;6.2), delivered over **MBS-4-MC** | MBSTF transmit; MBSF Client receive (both via rt-libflute) | Implemented &mdash; see the [rt-libflute audit](/reference-tools/multimedia/scope) for the transport-layer detail, including the Raptor FEC gap |
| MBS-6 / MBS-7 exposure to the MBS-Aware Application | MBSF Client | Implemented as local HTTP APIs: list announced services, activate/deactivate reception, inspect the received-content store, fetch received objects at their original Content-Location paths |
| MBS-Aware Application | rt-mbs-application | Implemented (dashboard: announced services with play/deactivate, received-object cache, decomposed announcement bundle) |
| MBS-4-UC (unicast object repair) | &mdash; | Not implemented (no unicast repair path; also outside TS 29.581's own scope) |
| File repair / reception reporting (associated delivery procedures) | &mdash; | Not implemented |

:::note[MBS-4-MC is the only over-the-air announcement path today]
Of the two modes that deliver an announcement to a client at all, only the multicast/broadcast one (MBS-4-MC) is implemented. There is no unicast fallback (MBS-5): a client that cannot receive the broadcast carousel has no way to discover services.
:::

### TS 29.581 &mdash; MBSTF distribution session services at Nmb2

Server: **MBSTF (rt-mbs-transport-function)**. Consumer: **MBSF (rt-mbs-function)**. Sits between the MBSF and the MBSTF.

| Service operation / feature | Status |
| --- | --- |
| Create Distribution Session (POST `/dist-sessions`) | Implemented |
| Retrieve Distribution Session (GET `.../{distSessionRef}`) | Implemented |
| Update Distribution Session (PATCH, RFC 6902 JSON Patch) | Implemented (state-only activate/deactivate and reconfiguration) |
| Destroy Distribution Session (DELETE) | Implemented |
| StatusSubscribe / StatusUnsubscribe (`.../subscriptions`) | Implemented (create, update, delete) |
| StatusNotify (events to the MBSF callback) | Implemented (failed notifications retried, not dropped) |
| Object distribution method: `SINGLE` operating mode, pull and push acquisition (per-session push-ingest endpoint advertised as `objIngestBaseUrl`) | Implemented |
| Object distribution method: `STREAMING` operating mode (MPEG-DASH MPD, live profile, pull and push) | Implemented |
| Object distribution method: `CAROUSEL` operating mode (`application/3gpp-mbs-object-manifest+json`; runs the Service Announcement channel) | Implemented |
| Object distribution method: `COLLECTION` operating mode | Not implemented |
| Streaming manifest types other than MPEG-DASH MPD (e.g. HLS) | Not implemented (delivered as plain objects, not parsed for scheduling) |
| Packet distribution method: `PACKET_FORWARD_ONLY` and `PACKET_PROXY` operating modes | Implemented |
| FLUTE/ALC delivery towards the MB-UPF at Nmb9 (unicast UDP tunnel to the MB-SMF-assigned ingress address, path-MTU detection) | Implemented |
| AL-FEC beyond Compact No-Code (Raptor, mandatory for download receivers per TS 26.346 &sect;7.2.2) | Not implemented in the transmission path (available in rt-libflute; not yet wired in) |

### TS 29.532 &mdash; MB-SMF session management services at Nmb1

Server: **MB-SMF (Open5GS fork)**. Consumer: **MBSF**, via the [5GC Service Consumers](../5g-core) library. Sits between the MBSF and the 5G Core.

| Service operation | Status |
| --- | --- |
| Nmbsmf_TMGI: Allocate (including refresh of previously allocated TMGIs) | Implemented |
| Nmbsmf_TMGI: Deallocate (specific list or all; TMGIs referenced by a live MBS session are protected) | Implemented |
| Nmbsmf_MBSSession: Create (broadcast service type) | Implemented |
| Nmbsmf_MBSSession: Update (&sect;5.3.2.3, activity status) | Implemented |
| Nmbsmf_MBSSession: Release | Implemented |
| Nmbsmf_MBSSession: StatusSubscribe / StatusNotify | Not implemented |

### TS 29.518 &mdash; Namf_MBSBroadcast

Server: **AMF (Open5GS fork)**. Consumer: **MB-SMF (Open5GS fork)**. Sits between the MB-SMF and the AMF inside the 5G Core.

| Service operation | Status |
| --- | --- |
| ContextCreate (&sect;5.6.2.2) &mdash; drives NGAP Broadcast Session Setup towards the gNB | Implemented |
| ContextUpdate (broadcast session modification) | Not implemented |
| ContextRelease &mdash; drives NGAP Broadcast Session Release | Implemented |
| ContextStatusNotify | Not implemented |

### TS 29.244 &mdash; N4mb PFCP

Between **MB-SMF** (control plane) and **MB-UPF** (user plane), both in the Open5GS fork.

| Procedure / IE | Status |
| --- | --- |
| PFCP Session Establishment / Modification / Deletion with the Release-17 MBS IEs | Implemented |
| Local Ingress Tunnel (&sect;8.2.209; the Nmb9 ingest address returned to the MBSF) | Implemented |
| Common TEID / FSSM shared-delivery forwarding behaviour (&sect;8.2.207, &sect;5.34.2.2) | Implemented |
| PFCP Session Report for MBS sessions | Not implemented |

### TS 38.413 &mdash; NGAP broadcast procedures

Between the **AMF (Open5GS fork)** and the **gNB CU-CP (srsRAN Project fork)**, over N2.

| Procedure | Status |
| --- | --- |
| Broadcast Session Setup (Request/Response, with the MBS Session Setup Request Transfer carrying TMGI and shared-delivery tunnel information) | Implemented on both ends, dynamically (no pre-provisioned session configuration required) |
| Broadcast Session Release | Implemented on both ends |
| Broadcast Session Modification | Not implemented |
| Broadcast Session Release Required (gNB-initiated) | Not implemented |
| Distribution Setup/Release, Multicast Session Activation/Deactivation/Update, Multicast Group Paging | Not implemented (multicast mode) |

### TS 38.463 / TS 38.473 &mdash; E1AP and F1AP broadcast contexts

Inside the **gNB (srsRAN Project fork)**: E1AP between CU-CP and CU-UP, F1AP between CU and DU.

| Procedure | Status |
| --- | --- |
| E1AP BC Bearer Context Setup (with failure reporting) | Implemented |
| E1AP BC Bearer Context Modification (per-MRB F1-U tunnel information) | Implemented |
| E1AP BC Bearer Context Release | Implemented |
| F1AP Broadcast Context Setup (with per-MRB setup-failure reporting) | Implemented |
| F1AP Broadcast Context Release | Implemented |
| F1AP Broadcast Context Modification | Not implemented (session reconfiguration is handled DU-internally) |
| E1AP MC Bearer Context / F1AP multicast procedures | Not implemented (multicast mode) |

### TS 38.331 / TS 38.321 / TS 38.322 / TS 38.323 &mdash; Uu broadcast radio protocols

Between the **gNB (srsRAN Project fork)** transmitting and the **UE (`srsue`, srsRAN 4G fork)** receiving.

| Feature | gNB (transmit) | UE (receive) |
| --- | --- | --- |
| SIB20 (MCCH configuration, TS 38.331) | Implemented | Implemented |
| MCCH: MBSBroadcastConfiguration-r17 covering every live session's MRBs, transmission windows | Implemented | Implemented (with periodic re-acquisition, so sessions created later are still discovered) |
| MCCH change notification (DCI 4-0) | Implemented | &mdash; |
| MTCH scheduling / reception per session G-RNTI, concurrent sessions independently served | Implemented | Implemented (multiple simultaneous G-RNTIs, per-G-RNTI HARQ state) |
| MRB LCID management | DU-wide unique LCIDs across concurrent sessions | All MRBs of all advertised sessions wired |
| RLC-UM (TS 38.322) | Implemented | Implemented |
| PDCP broadcast MRB (TS 38.323; 18-bit SN) | Implemented | Implemented, with the &sect;5.2.1 SN-based receive-window bootstrap (a UE tuning in mid-stream has no COUNT synchronisation) |
| Delivery of received multicast IP to the MBSF Client | &mdash; | Implemented (TUN interface) |
| Multicast MRBs, PTP/PTM switching, MBS Interest Indication, group paging | Not implemented | Not implemented |

### User plane path

| Segment | Between | Status |
| --- | --- | --- |
| Nmb8 object ingest (pull or push) | MBS Application Provider &rarr; MBSTF | Implemented |
| Nmb9 FLUTE/ALC unicast tunnel | MBSTF &rarr; MB-UPF | Implemented |
| N6mb direct multicast ingest (alternative to the MBSTF path) | AF/AS &rarr; MB-UPF | Implemented |
| N3mb shared GTP-U delivery, with the mandatory PDU Session Container extension header (TS 38.415) on every downlink G-PDU | MB-UPF &rarr; gNB(s) | Implemented |
| Uu PTM broadcast (MCCH/MTCH) | gNB &rarr; UEs | Implemented |
| MBS-4-MC FLUTE reception (including a raw-capture relay for software TUN radio links) | UE &rarr; MBSF Client | Implemented |

### Out of scope today (all components)

- **MBS Multicast delivery mode** (TS 23.247): multicast session join via the SMF, PTP/PTM switching, group paging &mdash; see the caution above.
- **MB-PCF policy control** (TS 29.537), **NEF exposure** (TS 29.522 / Nmb5), **MBS security** (TS 33.501 traffic keys &mdash; delivery is unprotected), **charging** (TS 32.279).
- **MBS-specific NRF NFDiscovery query extensions** (TS 29.510) &mdash; standard NF registration/discovery is used; discovery by TMGI or MBS-session parameters is not.
- **Location-dependent (multi-area) MBS sessions** and **Xn mobility / service continuity**.

## How the reference tools map to the standard

It helps to read the audit above against the three-layer MBS architecture (covered in full on the [Tech portal](/tech/5g-mbs)):

- On the **user-service layer**, the MBSF provisions services and drives MBSTF distribution sessions (Nmb2); the MBSTF ingests objects from the MBS Application Provider at Nmb8 (pull or push) and delivers them as FLUTE/ALC packets towards the MB-UPF at Nmb9; the MBSF Client receives and reassembles them on the UE side and hands them to an MBS-Aware Application. Service Announcement over a broadcast carousel closes the discovery loop.
- On the **5G Core layer**, the MB-SMF manages TMGIs and MBS sessions (Nmb1), coordinates the MB-UPF over N4mb PFCP, and triggers broadcast session setup through the AMF (`Namf_MBSBroadcast` &rarr; NGAP). Direct multicast ingest into the MB-UPF at N6mb is available as an alternative to the MBSTF path.
- On the **NG-RAN layer**, the gNB terminates the shared N3mb delivery, sets up MRBs across CU-CP/CU-UP/DU, and broadcasts MCCH/MTCH; the UE acquires SIB20/MCCH and receives the MTCH point-to-multipoint.

The audit above marks which service operations and distribution modes are implemented and which are not, so use it to check whether a given path (for example the `COLLECTION` operating mode, or multicast session join) is covered before building on it.

## Standards & Specifications

The specifications audited above are consolidated below.

| Specification | Body | Reference |
| -------------- | ---- | --------- |
| TS 26.502 - MBS User Services architecture | 3GPP | [https://www.3gpp.org/dynareport/26502.htm](https://www.3gpp.org/dynareport/26502.htm) |
| TS 26.517 - MBS User Services protocols and formats | 3GPP | [https://www.3gpp.org/dynareport/26517.htm](https://www.3gpp.org/dynareport/26517.htm) |
| TS 29.580 - MBSF services (Nmb10) | 3GPP | [https://www.3gpp.org/dynareport/29580.htm](https://www.3gpp.org/dynareport/29580.htm) |
| TS 29.581 - MBSTF services (Nmb2) | 3GPP | [https://www.3gpp.org/dynareport/29581.htm](https://www.3gpp.org/dynareport/29581.htm) |
| TS 29.532 - MB-SMF session management services (Nmb1) | 3GPP | [https://www.3gpp.org/dynareport/29532.htm](https://www.3gpp.org/dynareport/29532.htm) |
| TS 29.244 - PFCP (N4mb) | 3GPP | [https://www.3gpp.org/dynareport/29244.htm](https://www.3gpp.org/dynareport/29244.htm) |
| TS 29.518 - AMF services incl. Namf_MBSBroadcast | 3GPP | [https://www.3gpp.org/dynareport/29518.htm](https://www.3gpp.org/dynareport/29518.htm) |
| TS 38.413 - NGAP broadcast session management | 3GPP | [https://www.3gpp.org/dynareport/38413.htm](https://www.3gpp.org/dynareport/38413.htm) |
| TS 38.463 / TS 38.473 - E1AP / F1AP broadcast contexts | 3GPP | [TS 38.463](https://www.3gpp.org/dynareport/38463.htm), [TS 38.473](https://www.3gpp.org/dynareport/38473.htm) |
| TS 38.331 / TS 38.32x - NR RRC (SIB20/MCCH), MAC/RLC/PDCP | 3GPP | [TS 38.331](https://www.3gpp.org/dynareport/38331.htm) |
| TS 23.247 - MBS architecture (Stage 2) | 3GPP | [https://www.3gpp.org/dynareport/23247.htm](https://www.3gpp.org/dynareport/23247.htm) |
| TS 29.510 - NRF NFDiscovery (MBS query extensions not implemented) | 3GPP | [https://www.3gpp.org/dynareport/29510.htm](https://www.3gpp.org/dynareport/29510.htm) |

For the full standards landscape, see [Standards: 5G Multicast Broadcast Services](/standards/5g-mbs); for the FLUTE transport underneath the Object distribution method (and why it is FLUTE version 1), see [Standards: Multimedia Delivery Protocols](/standards/multimedia).

## Getting Started

To try the tools hands-on, follow the [Tutorials](./tutorials): they cover the two ways to provision the 5GC (direct MBS core functions, or via the [5GC Service Consumers](../5g-core) libraries), the [Docker-Compose deployment](./tutorials/docker-implementation), and the individual MBSF/MBSTF walkthroughs. For the source code, packages and releases behind each component, see [Resources](./resources); for the specification background, see [Standards: 5G Multicast Broadcast Services](/standards/5g-mbs).

## NTN deployment context

The 5G MBS reference tools are also the relevant software for **Non-Terrestrial Network (NTN)** deployment scenarios. NTN extends 5G MBS coverage via GEO and LEO satellites and introduces specific challenges around propagation delay, Doppler effects, and satellite beam handover. The MBS Multicast and Broadcast delivery modes operate over NTN access without changes to the MBS service layer itself. Technical analysis of these scenarios is available on the Tech portal:

- [Tech: MBS Multicast over NTN](/tech/ntn/analysis-mbs-multicast-over-ntn)
- [Tech: MBS Broadcast over NTN](/tech/ntn/analysis-mbs-broadcast-over-ntn)
- [Tech: Non-Terrestrial Networks (overview)](/tech/ntn)

## Roadmap

This page tracks the roadmap for the 5G Multicast Broadcast Services (5MBS) reference tools. The work is organised into two workstreams that build on each other: first the 5G Core (5GC) functions (MB-SMF, MB-UPF, AMF and basic multicast support), then the MBS User Services layer (MBSF, MBSTF and the MBS Application Function/Application Server) that runs on top of them.

### General Project Roadmap

The Kanban board below tracks the planned and in-progress work for the 5G Multicast Broadcast Services (5MBS) reference tools (5G Core multicast functions and the MBS User Services layer). It is maintained live on GitHub.

[Kanban board: Roadmap](https://github.com/orgs/5G-MAG/projects/48/views/13)

### Developer Kanban Boards

#### 5MBS: 5G Core functions

Initial implementation of MB-SMF, MB-UPF, AMF and basic multicast support in the 5G Core

- [**Kanban board**](https://github.com/orgs/5G-MAG/projects/19/views/2)

#### 5MBS: MBS User Services

Initial implementation of MBSF, MBSTF, MBS AF and MBS AS for supporting media multicast/broadcast. This builds on top of the 5G Core functions indicated above.

- [**Kanban board**](https://github.com/orgs/5G-MAG/projects/43/views/1)

## Related

- [Resources](/reference-tools/5g-mbs/resources)
- [Tutorials](/reference-tools/5g-mbs/tutorials)
- Standards: [5G Multicast Broadcast Services](/standards/5g-mbs)
- Tech: [Multicast and Broadcast Services in 5G Networks](/tech/5g-mbs)
